# 项目规范合规自测报告

## 一键启动
- [x] 通过 `docker compose up --build -d` 一键启动全部服务。
- [x] 依赖均在 Dockerfile / docker-compose 中声明。
- [x] 数据库 schema 在应用启动时自动创建，无需手工建表。

## 环境隔离与零人工干预
- [x] 未依赖宿主机路径或本地全局 Python/Node 包。
- [x] 注册、问卷、匹配、邮件全链路在容器内可跑通。
- [x] 无需手动复制 `.env` 或人工输入交互式配置。

## 核心业务真实实现（拒绝 Mock）
- [x] 实现 ROSE 50 题评分、逆向计分、四维阈值与平局熔断。
- [x] 实现硬过滤（档案互选、I/S 错位、Q1/Q41/Q42 一票否决）。
- [x] 实现加权距离、互补加分、归一化匹配度与 99.9 封顶。
- [x] 实现每周二 21:00（Asia/Shanghai）自动匹配调度与邮件通知。

## 工程质量
- [x] 后端保持路由 / 业务逻辑 / 数据层分离。
- [x] 参数校验与标准化 JSON 返回已覆盖核心接口。
- [x] 错误不透传堆栈给前端（500 返回统一信息）。
- [x] 前端关键提交按钮具备 Loading 与禁用防抖。

## 测试标准
- [x] 提供 `unit_tests/`（ROSE 逻辑与边界）。
- [x] 提供 `API_tests/`（注册->登录->问卷->匹配全链路）。
- [x] 提供 `run_tests.sh` 一键执行并输出 `Total/Passed/Failed` 汇总。

## README 一致性
- [x] 已更新 `How to Run`、`Services List`、`Verification`。
- [x] 文档中的端口、路径、流程与当前代码实现一致。
