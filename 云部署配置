# 1.0 vs 云部署配置调整：逐条差异清单

目标：把 **1.0（commit `7faa33e`）** 的部署方式，手工对齐到 **云部署配置调整（commit `e4ca22b`）** 的部署方式。  
范围：只包含 `e4ca22b` 这一次提交引入的差异（不包含后续 `df059fd` 等业务改动）。

## 总览

对比范围：`7faa33e..e4ca22b`

- **修改 4 个文件**
  - `README.md`
  - `docker-compose.yml`
  - `backend/Dockerfile`
  - `frontend/Dockerfile`
- **删除 3 个文件**
  - `nginx/conf.d/default.conf`
  - `nginx/certs/jlu.key`
  - `nginx/certs/jlu.pem`

变更统计：**7 files changed, +37 / -146**

---

## 1) README.md（文档/运维指导差异）

### 1.1 端口说明调整

- **前端端口**：从 `8383` 改为 `8080`
  - 原文：`Frontend | 8383 | http://localhost:8383`
  - 现文：`Frontend | 8080 | http://localhost:8080`

### 1.2 新增：Cloudflare + 香港服务器上线说明（推荐）

新增一整段 “Cloudflare + 香港服务器上线（推荐）” 指南，核心要点：

- **服务器端口开放建议**
  - 必开：`80/tcp`、`443/tcp`
  - 可选：`8000/tcp`（仅用于直连后端调试）

- **根目录 `.env` 建议项**
  - `WEB_BASE_URL=https://你的域名`
  - `FRONTEND_PORT=8080`（让宿主机 Nginx 占用 `80/443`，避免和容器冲突）
  - `NPM_REGISTRY=https://registry.npmjs.org/`
  - `FORCE_HTTPS=false`（先由 Cloudflare 做 HTTPS，稳定后再考虑源站强制）

- **Cloudflare DNS 建议**
  - `A` 记录 → 香港服务器公网 IP
  - 代理状态：建议先开橙云（Proxied）

- **Cloudflare SSL/TLS 建议**
  - 先用 `Full`
  - 源站证书部署好后切 `Full (strict)`

- **启动命令与验证建议**
  - `docker compose up --build -d`
  - 验证入口：`/` 与 `/api/public/site-settings`
  - 若宿主机已有 Nginx 反代：`proxy_pass http://127.0.0.1:8080`

### 1.3 验证与管理页地址同步改端口

文内所有前端入口与管理页地址：

- `http://localhost:8383` → `http://localhost:8080`
- `http://localhost:8383/admin` → `http://localhost:8080/admin`

---

## 2) backend/Dockerfile（后端构建差异）

### 2.1 npm registry 配置方式变化

1.0 版本做法（固定改镜像）：

- `RUN npm config set registry https://registry.npmmirror.com/ || true`
- `RUN npm install`

云部署配置调整版做法（可配置/默认官方源）：

- 新增：`ARG NPM_REGISTRY=https://registry.npmjs.org/`
- 改为：`RUN npm install --registry=${NPM_REGISTRY}`

### 2.2 意图/效果

- 云上/海外构建更稳定（默认走官方 registry）
- 允许你在 `.env` 或构建参数里替换为镜像源（加速、或适配网络）

---

## 3) frontend/Dockerfile（前端构建差异）

### 3.1 npm registry 配置方式变化

1.0 版本做法（固定改镜像）：

- `RUN npm config set registry https://registry.npmmirror.com/ || true`
- `RUN npm install`

云部署配置调整版做法（可配置/默认官方源）：

- 新增：`ARG NPM_REGISTRY=https://registry.npmjs.org/`

### 3.2 前端特殊处理：删除 package-lock.json 再安装

新增逻辑：

- `RUN rm -f package-lock.json && npm install --registry=${NPM_REGISTRY}`

意图（来自提交注释与实际用途）：

- 有些 `package-lock.json` 可能锁定了区域镜像 URL（比如 npmmirror），云上拉取会失败
- 在容器内重新生成 lock，避免被镜像源“绑死”

⚠️ 影响提示：

- 这会导致容器内安装的依赖版本可能与本地 lock 不完全一致（尤其是 semver 允许范围内）
- 如果你非常在意可重复构建，建议后续再专门处理 lock 的策略（但本 diff 的目标是复现 `e4ca22b` 行为）

---

## 4) docker-compose.yml（部署拓扑差异，最关键）

### 4.1 backend 构建方式变化（支持 NPM_REGISTRY）

1.0：

- `backend: build: ./backend`

云部署配置调整：

- `backend: build:`
  - `context: ./backend`
  - `args:`
    - `NPM_REGISTRY=${NPM_REGISTRY:-https://registry.npmjs.org/}`

### 4.2 frontend 构建方式变化（支持 NPM_REGISTRY）

1.0：

- `frontend: build: ./frontend`

云部署配置调整：

- `frontend: build:`
  - `context: ./frontend`
  - `args:`
    - `NPM_REGISTRY=${NPM_REGISTRY:-https://registry.npmjs.org/}`

### 4.3 删除 nginx 服务（HTTPS/反代由 Cloudflare 或宿主机处理）

1.0 存在一个 `nginx` 服务，特点是：

- `image: nginx:alpine`
- 端口：`80:80`、`443:443`
- 挂载：
  - `./nginx/conf.d:/etc/nginx/conf.d:ro`
  - `./nginx/certs:/etc/nginx/certs:ro`

云部署配置调整版：

- **彻底删除 `nginx` 服务**
- 也就不再需要 `./nginx/` 目录下那套证书与配置

### 4.4 frontend 暴露端口从“固定 80”变为“可配置 FRONTEND_PORT”

1.0：

- 端口固定（由 nginx 对外暴露 80/443）

云部署配置调整：

- `frontend.ports: - "${FRONTEND_PORT:-80}:80"`

意图：

- 如果宿主机要自己跑 Nginx 占用 `80/443`，你可以让容器前端改走 `8080`（避免冲突）
- 配合 Cloudflare 反代时也更灵活

---

## 5) nginx/ 目录（证书/配置差异）

云部署配置调整版删除了 3 个文件：

- `nginx/conf.d/default.conf`
- `nginx/certs/jlu.key`
- `nginx/certs/jlu.pem`

含义：

- **不再在仓库内存放 TLS 私钥/证书**
- HTTPS 终止点改为 Cloudflare 或宿主机 Nginx（推荐），容器内不再负责 443

⚠️ 安全提示：

- 1.0 把私钥放进仓库风险很高（即使是 origin cert 也不应进 Git）
- 这个删除属于“正确方向”

---

## 6) 你在本地代码上需要做的“手工对齐操作”（按步骤）

如果你当前“产品大改”是基于 1.0 修改的，且你只想补齐云部署差异，请按下面做：

1. 修改 `docker-compose.yml`
   - backend/frontend 的 build 改为 `context + args(NPM_REGISTRY)`
   - 删除 `nginx` service（如果存在）
   - `frontend.ports` 改为 `"${FRONTEND_PORT:-80}:80"`
2. 修改 `backend/Dockerfile`
   - 去掉固定镜像源设置
   - 增加 `ARG NPM_REGISTRY`，并在 `npm install` 时使用它
3. 修改 `frontend/Dockerfile`
   - 增加 `ARG NPM_REGISTRY`
   - 增加 `rm -f package-lock.json && npm install --registry=...`（按 `e4ca22b` 行为）
4. 删除 `nginx/` 目录（或至少不再引用、并确保不包含私钥/证书）
5. 更新 `README.md`（可选但推荐）
   - 端口统一到 `8080`
   - 加入 Cloudflare/宿主机 Nginx 指引，便于以后部署

---

## 7) 部署验证清单（建议）

在服务器上（或本地）验证云部署形态：

- `.env` 至少设置：
  - `POSTGRES_PASSWORD`、`SECRET_KEY`、`PRIVACY_KEYRING_JSON`
  - 若宿主机已占用 80：设置 `FRONTEND_PORT=8080`
  - 如需：`NPM_REGISTRY=https://registry.npmjs.org/`
- 启动：
  - `docker-compose up -d --build`
- 检查：
  - `docker-compose ps`
  - `docker-compose logs --tail=200 backend`
  - 访问前端首页与 `/api/...` 相关接口
